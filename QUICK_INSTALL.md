# راهنمای سریع نصب برنامه صدی روی موبایل

## ✅ وضعیت فعلی:
- ✅ موبایل متصل است: `RZCTB0A5VTD`
- ✅ برنامه در حال اجراست

## 📱 روش‌های نصب:

### روش 1: اجرای مستقیم (در حال اجرا)
برنامه در حال build و اجرا روی موبایل شماست.

**اگر پیام تایید دانلود منابع را دیدید:**
- فقط `y` را تایپ کنید و Enter بزنید

### روش 2: ساخت APK و نصب دستی

#### مرحله 1: ساخت APK
```powershell
flutter build apk --debug
```
**اگر پیام تایید دیدید، `y` را بزنید**

#### مرحله 2: نصب APK
```powershell
adb install build\app\outputs\flutter-apk\app-debug.apk
```

یا فایل `build\app\outputs\flutter-apk\app-debug.apk` را به موبایل منتقل کنید و دستی نصب کنید.

## 🔧 دستورات مفید:

### بررسی دستگاه‌های متصل:
```powershell
adb devices
```

### اجرای برنامه روی موبایل:
```powershell
flutter run -d RZCTB0A5VTD -t lib/main.dart
```

### ساخت APK:
```powershell
flutter build apk --debug
```

### نصب APK:
```powershell
adb install build\app\outputs\flutter-apk\app-debug.apk
```

## ⚠️ نکات مهم:

1. **USB Debugging باید فعال باشد** در تنظیمات Developer Options موبایل
2. **مجوز USB Debugging** را در موبایل تایید کنید
3. **موبایل باید با USB وصل باشد**

## 🎯 وضعیت فعلی:
برنامه در حال اجراست روی موبایل شما! منتظر بمانید تا build کامل شود.

