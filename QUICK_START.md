# راهنمای سریع اجرای برنامه

## 🚀 اجرای سریع

### روش 1: استفاده از اسکریپت راهنما (پیشنهادی)

**برای PowerShell:**
```powershell
.\run-interactive.ps1
```

این اسکریپت راهنمای کامل برای اجرای برنامه را نمایش می‌دهد.

### روش 2: استفاده از اسکریپت عادی

**برای PowerShell:**
```powershell
.\run.ps1
```

**برای Command Prompt:**
```cmd
run.bat
```

### روش 2: اجرای دستی

```bash
flutter run -t lib/main.dart
```

**⚠️ نکته مهم:** اگر پیام زیر را دیدید:
```
Flutter assets will be downloaded from https://storage.googleapis.com. 
Make sure you trust this source!
```

**فقط `y` را تایپ کنید و Enter بزنید.** این پیام فقط یک بار نمایش داده می‌شود.

## 📱 بررسی دستگاه‌های متصل

قبل از اجرا، می‌توانید دستگاه‌های متصل را ببینید:

```bash
flutter devices
```

## ✅ حل مشکل دانلود منابع

اگر همیشه این پیام را می‌بینید، می‌توانید منابع را از قبل دانلود کنید:

```bash
# در PowerShell
echo y | flutter precache

# یا مستقیماً در ترمینال
flutter precache
# سپس "y" را تایپ کنید
```

## 🎯 نکات مهم

1. **امولاتور باید باز باشد** قبل از اجرای برنامه
2. **اینترنت باید متصل باشد** برای دانلود منابع (فقط بار اول)
3. **برنامه در حالت لوکال اجرا می‌شود** - نیازی به بک‌اند نیست

## 🐛 حل مشکلات رایج

### مشکل: "No devices found"
- مطمئن شوید امولاتور Android باز است
- یا یک دستگاه فیزیکی متصل است

### مشکل: "Flutter assets will be downloaded"
- این یک پیام امنیتی است، نه خطا
- فقط `y` را تایپ کنید

### مشکل: برنامه اجرا نمی‌شود
```bash
# پاک کردن کش
flutter clean

# نصب مجدد وابستگی‌ها
flutter pub get

# اجرای مجدد
flutter run
```

